# Site Change Log

## 2026-03-20

### Automotive booking page MVP

- Added a dedicated automotive entry page at `/automotive` instead of forcing this into the main industrial landing page.
- Chose a separate Vite HTML entry over `react-router-dom` because this repo already uses a multi-page build (`index.html` + `apply.html`), and the standalone entry keeps metadata, sitemap coverage, and deployment behaviour simpler.
- Built the automotive experience as modular React sections in `src/sections/automotive/` with a shared shell in `src/layouts/AutomotiveSite.tsx`.
- Added a four-step booking wizard with shared service-selection state, SGI-specific mailing-address handling, dynamic weekday slot generation, and booking analytics events.
- Reused the existing `LegalModal` pattern by extracting shared privacy and terms content into `src/components/legalContent.tsx`.
- Added a shared booking contract plus `api/automotive-booking.ts` as the first Vercel booking handoff path, with optional webhook forwarding and a localhost preview fallback for non-Vercel local work.
- Centralized shared company contact details in `src/data/company.ts`, moved the SGI badge into `public/images/`, and converted the SGI vehicle-size selector to real radio inputs.

## 2026-03-09

### Careers hub simplification

- Replaced the heavier careers section with a lighter hub in `src/sections/CareersForge.tsx`.
- Moved `Current Hiring Focus` above the future-opportunities intake so applicants see live jobs first.
- Removed the featured opportunity panel to reduce intimidation and vertical space.
- Routed each live role to `apply.html`, where the full application intake lives.
- Updated `vite.config.ts` to build both `index.html` and `apply.html`.

### Partner reel logo fix

- The trusted partners reel in `src/sections/ClientStream.tsx` was using raw `/logos/...` string paths.
- That approach is fragile when the app is served from a different base, previewed from a separate entry point, or cached inconsistently during local work.
- The reel now imports each logo as a Vite-managed asset from `src/assets/client-logos/`.
- This keeps the runtime URLs generated by the bundler instead of depending on hard-coded public paths.
- Obsidian requires a brand-specific horizontal offset because the visible mark sits left-of-center inside the source SVG viewBox.
- The current Obsidian correction is `logoShiftX: '18px'` and `logoShiftY: '-28px'` in `src/sections/ClientStream.tsx`.

### Verification notes

- `npm run build`
- Browser check on the home page with Playwright against `http://127.0.0.1:4173/`
- Confirmed the partner reel renders image elements after the asset change

### Follow-up items

- There are still separate references to `/MPS Logo.png` elsewhere in the site that should be normalized if favicon or header logo issues show up again.
- Playwright still reports existing `<circle>` attribute console errors from another animated section. That issue is separate from the partner reel asset loading fix.
